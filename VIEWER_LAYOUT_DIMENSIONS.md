# Viewer 新版版面尺寸

新版觀眾畫面的主要尺寸如下：

| 項目 | 寬度 | 高度 |
| --- | ---: | ---: |
| `.overlay` | `min(90vw, 1720px)` | `46vh` |
| `.top-info` | 容器全寬 | 最少 `96px` |
| `.coach-grid` | 容器全寬 | 填滿剩餘高度 |
| 單一 `.coach-card` | `(overlay 寬度 - 42px) / 4` | `(coach-grid 高度 - 14px) / 2` |
| 教練卡片頭像 | `72px` | `72px` |
| 得標結果區塊 | 容器全寬 | 填滿上方資訊列以下的剩餘高度 |
| 得標教練頭像 | `110px` | `110px` |

其他間距：

- 畫面底部至 `.overlay`：`4px`
- `.overlay` 上內距：`14px`
- `.overlay` 下內距：`4px`
- 區塊與卡片間距：`14px`
- 單張教練卡片內距：上下 `10px`、左右 `14px`

## 1920 × 1080 畫面參考值

在瀏覽器縮放 100%、視窗為 1920 × 1080，且上方資訊列維持其內容所需的 `100px` 高度時：

| 項目 | 寬度 | 高度 |
| --- | ---: | ---: |
| `.overlay` | `1720px` | `496.8px` |
| `.coach-grid` | `1720px` | 約 `364.8px` |
| 單一 `.coach-card` | 約 `419.5px` | 約 `175.4px` |

實際像素值可能因視窗尺寸、瀏覽器子像素取整與上方資訊內容略有差異。

## 動畫重新設計版本（沿用原繪圖尺寸）

`redesign-viewer-overlay` 分支將九個主要方塊恢復成原版尺寸：

| 項目 | 寬度 | 高度 |
| --- | ---: | ---: |
| 九格整體範圍 | `1720px` | `376px` |
| 上方競標資訊框 | `1720px` | `102px` |
| 單張教練卡片 | 約 `419.5px` | `114px` |
| 外層下內距 | — | `4px` |

上述數值以 1920 × 1080、瀏覽器縮放 100% 為準。

## Results 畫面

| 項目 | 數值 |
| --- | ---: |
| 整體結果範圍 | `1720 × 428px` |
| 單張教練結果卡 | 約 `419.5 × 200px` |
| 卡片內距 | `12px` |
| 卡片間距 | `14px` |
| 畫面底部至外層 | `0px` |
| 外層下內距 | `0px` |

卡片內容至畫面底部不再保留額外空隙。
